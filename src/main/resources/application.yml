# Spring Boot configuration
spring:
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver

    #########################################################################################
    url: jdbc:postgresql://flora.db.elephantsql.com/lykzract
    username: lykzract
    password: Z0iVSPAjp_9_u8d3NnPwP29jvVDdiTtP
    #########################################################################################
    
  # JPA properties
  jpa:
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
# Server configuration
server:
  port: 8080
# Security configuration
security:
  user:
    name: user
password: user
